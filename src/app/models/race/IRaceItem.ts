// import { IOddGroup, Dynamics } from './../';
// import { IObservableFactories, IObservableObject } from 'mobx';
import { IOddItem } from './../';

export interface IRaceItem extends IOddItem {
  __?: boolean; // hack
}
